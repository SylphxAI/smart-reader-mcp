{
  "name": "@sylphx/smart-reader-mcp",
  "version": "0.2.1",
  "mcpName": "io.github.SylphxAI/smart-reader-mcp",
  "description": "One MCP call reads PDF, image, or video by sniffing format and delegating to Sylphx Reader siblings.",
  "type": "module",
  "publishConfig": {
    "access": "public"
  },
  "license": "MIT",
  "bin": {
    "smart-reader-mcp": "./bin/smart-reader-mcp"
  },
  "files": [
    "bin/",
    "dist/",
    "README.md",
    "LICENSE"
  ],
  "exports": {
    ".": "./dist/index.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SylphxAI/smart-reader-mcp.git"
  },
  "homepage": "https://github.com/SylphxAI/smart-reader-mcp#readme",
  "keywords": [
    "mcp",
    "model-context-protocol",
    "reader",
    "evidence",
    "sylphx",
    "pdf",
    "image",
    "video"
  ],
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "build:rust": "cargo build --release -p smart-reader-core -p smart-reader-cli -p smart-reader-mcp-server && bun scripts/stage-rust-mcp.ts",
    "test:rust": "cargo test",
    "build": "tsc --project tsconfig.build.json",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "test:rust-sniff": "cargo test && bun test test/rustSniff.boundary.test.ts",
    "test:watch": "bun test --watch",
    "check": "biome check .",
    "check:fix": "biome check --write .",
    "sync:server-json": "bun scripts/sync-server-json.ts",
    "sync:platform-versions": "bun scripts/sync-platform-versions.ts",
    "assemble:multiarch": "bun scripts/assemble-multiarch-natives.ts",
    "verify:multiarch-readback": "bun scripts/verify-multiarch-readback.ts",
    "version-packages": "changeset version && bun run sync:server-json && bun run sync:platform-versions",
    "validate": "bun run check && bun run typecheck && bun run build && bun run test",
    "doctor": "bun run src/index.ts doctor",
    "benchmark:release-gate": "bun scripts/release-gate.ts",
    "clean": "rm -rf dist coverage",
    "prepublishOnly": "bun run clean && bun run build",
    "release": "bun scripts/sync-platform-versions.ts && bun scripts/publish-platform-packages.ts && bun run typecheck && bun run check && bun run build && bun test && changeset publish"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@sylphx/reader-evidence": "github:SylphxAI/reader-evidence",
    "zod": "^4.4.3"
  },
  "optionalDependencies": {
    "@sylphx/pdf-reader-mcp": "3.0.14",
    "@sylphx/image-reader-mcp": "0.1.0",
    "@sylphx/video-reader-mcp": "0.1.0",
    "@sylphx/smart-reader-mcp-darwin-arm64": "0.2.1",
    "@sylphx/smart-reader-mcp-darwin-x64": "0.2.1",
    "@sylphx/smart-reader-mcp-linux-x64-gnu": "0.2.1",
    "@sylphx/smart-reader-mcp-linux-arm64-gnu": "0.2.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.16",
    "@changesets/changelog-github": "^0.7.0",
    "@changesets/cli": "^2.31.0",
    "@types/node": "^25.9.1",
    "bun-types": "^1.3.12",
    "typescript": "^7.0.2"
  },
  "packageManager": "bun@1.3.12"
}
