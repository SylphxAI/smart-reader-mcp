node_modules/
build/
dist/
target/
*.log
coverage/
.env*

# VitePress
docs/.vitepress/cache
docs/.vitepress/dist

# Test Reports
test-report.junit.xml

# Intentionally tracked: test/fixtures/mislabeled/png-as-pdf.pdf (magic-byte corpus)
benchmark-artifacts/*.json

.vercel

# Staged host native (built via bun run build:rust or release assemble)
bin/native/*
!bin/native/.gitkeep

# Multi-arch assembled native binaries (built in CI)
npm/*/smart-reader-mcp-server
artifacts/
